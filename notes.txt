Component Degin:=

 Body Component / Main Component/Container
   NavBar
   Routes = / => feed
   Routes = /login => login page
   Routes = /connetions => connections lists
   Routes = /user/profile => User profiles Data

<Route path="/" element={<Body/>}/> is same
<Route path="/" element={<Body/>}> </Route>



Agar export const dikhe 👉 import mein {} lagao

Agar export default dikhe 👉 import mein {} mat lagao.

We Biuild Recently :-->
 You Sholud Not Be acces other routes without login
 if token is not present redirect user to the login page


Complete flow of request page

// create a store
// create a slice add to the store 
// fetch the data form the request and save it form the store
// and then show the request

